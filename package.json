{
  "name": "tanstack-start-lwa-monorepo",
  "private": true,
  "packageManager": "pnpm@10.24.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "pnpm -r --filter ./packages... run build",
    "lint": "biome check .",
    "cdk:deploy": "pnpm --filter infra run cdk:deploy",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
